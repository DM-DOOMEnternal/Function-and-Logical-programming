%–еализовать предикат fib(N,X), где X Ц число ‘ибоначчи с
%номером N, причем 1 и 2 элемент равны 1 с помощью рекурсии вверх.
fib(0,1):-!.
%fib(1,1):-!.
fib(1,2):-!.
fib(N,X):-N>1,
    N1 is N-1, N2 is N-2,
    fib(N1,X1),
    fib(N2,X2),
    X is X1+X2.



fact(0,1):- !.
fact(N,X):- N1 is N-1,fact(N1,X1), X is X1*N.
